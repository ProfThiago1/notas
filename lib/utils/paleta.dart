// arquivo para paleta de cores do app
import 'package:flutter/material.dart';

class AppColors {
  // Cores principais
  static const Color primary = Color(0xFF0057B8); // Azul vibrante
  static const Color secondary = Color(0xFF4FC3F7); // Azul claro
  static const Color accent = Color(0xFFF8C02D); // Amarelo vibrante

  // Estados
  static const Color success = Color.fromARGB(255, 59, 221, 65); // Verde sucesso
  static const Color warning = Color.fromARGB(255, 255, 167, 38); // Laranja atenção
  static const Color error = Color.fromARGB(255, 229, 57, 53);   // Vermelho erro

  // Neutros
  static const Color lightGray = Color.fromARGB(234, 252, 252, 252);
  static const Color darkGray = Color(0xFF424242);
  static const Color grafite = Color(0xFF212121);

}


class AppColorsDark {
  // Cores principais
  static const Color primary = Color(0xFF0057B8); // Azul vibrante
  static const Color secondary = Color(0xFF4FC3F7); // Azul claro
  static const Color accent = Color(0xFFF8C02D); // Amarelo vibrante

  // Estados
  static const Color success = Color.fromARGB(255, 59, 221, 65); // Verde sucesso
  static const Color warning = Color.fromARGB(255, 255, 167, 38); // Laranja atenção
  static const Color error = Color.fromARGB(255, 229, 57, 53);   // Vermelho erro

  // Neutros
  static const Color lightGray = Color.fromARGB(234, 252, 252, 252);
  static const Color darkGray = Color(0xFF424242);
 

}